import { Navigate } from "react-router-dom";

/** Ruta histórica: redirige al hub principal de la convención. */
export default function ConvencionCientifica2026() {
  return <Navigate to="/convencion" replace />;
}
